
@(Html.Kendo().Grid<OWL_Site.Models.Setting>()
      .Name("grid")
      .Columns(columns =>
      {
        columns.Bound(c => c.AuthDnAddress);
        columns.Bound(c => c.OU);
        columns.Bound(c => c.UserGroup);
        columns.Bound(c => c.AdminGroup);
        columns.Bound(c => c.DnAdminUn);
          columns.Bound(c => c.DnAdminPass);
        columns.Bound(c => c.CobaMngAddress);
        columns.Bound(c => c.CobaCfgAddress);
        columns.Bound(c => c.CobaRecordsAddress);
        columns.Bound(c => c.CobaRecLogin);
        columns.Bound(c => c.CobaRecPass);
        columns.Bound(c => c.CobaRecBdName);
        columns.Bound(c => c.CobaRecBdTable);
        columns.Bound(c => c.SmtpServer);
        columns.Bound(c => c.SmtpPort);
        columns.Bound(c => c.SmtpSSL);
        columns.Bound(c => c.SmtpLogin);
        columns.Bound(c => c.SmtpPassword);
        columns.Bound(c => c.MailFrom_email);
        columns.Bound(c => c.MailFrom_name);
        columns.Bound(c => c.CobaMngLogin);
        columns.Bound(c => c.CobaMngPass);
      })
      .Scrollable()
      .Resizable(resize => resize.Columns(true))
      .Mobile()
      .DataSource(dataSource => dataSource
          .Ajax()
          .Read(read => read.Action("Settings_Read", "Sets"))
      )
)
